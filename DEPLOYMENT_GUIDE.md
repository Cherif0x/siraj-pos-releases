# دليل النشر والتوزيع - سراج

## للمطورين والموزعين فقط

هذا الدليل يشرح كيفية نشر وتوزيع برنامج سراج للعملاء.

---

## 📦 ملفات البرنامج

### موقع الملفات المنشورة
```
C:\SIRAJE CSharp\bin\Release\net10.0-windows\win-x64\publish\
```

### الملفات الرئيسية
- `InventoryManagement.exe` - الملف التنفيذي
- `Resources/` - الموارد (خطوط، صور، ترجمات)
- `app.ico` - أيقونة البرنامج

---

## 🔐 نظام الترخيص

### أنواع التراخيص المتاحة

#### 1. تجربة مجانية
- **المدة:** 30 يوم
- **الميزات:** جميع الميزات متاحة
- **التفعيل:** تلقائي عند أول تشغيل

#### 2. ترخيص سنوي
- **المدة:** سنة كاملة (365 يوم)
- **الربط:** مرتبط بالجهاز
- **التجديد:** سنوي

### ⚠️ ملاحظة مهمة

> **لا يوجد ترخيص مدى الحياة**  
> فقط تجربة مجانية وترخيص سنوي

---

## 🔑 إنشاء التراخيص

### استخدام أداة إنشاء التراخيص

#### الخطوة 1: تشغيل الأداة

```powershell
cd "C:\SIRAJE CSharp\Siraje.LicenseGenerator\bin\Debug\net10.0-windows"
.\Siraje.LicenseGenerator.exe
```

#### الخطوة 2: اختيار المفتاح الخاص

1. اضغط **"Select Private Key File..."**
2. اختر ملف المفتاح الخاص: `private_key.xml`
3. ستظهر رسالة: "✓ Private key loaded"

> 🔒 **أمان المفتاح الخاص:**  
> المفتاح الخاص محفوظ خارج المشروع في مكان آمن.  
> لا ترفعه إلى GitHub أبداً!

#### الخطوة 3: إدخال بيانات الترخيص

1. **اسم العميل:** أدخل اسم المحل
2. **Hardware Hash:** انسخه من شاشة التفعيل عند العميل
3. **نوع الترخيص:** اختر **Annual** (سنوي فقط)
4. **تاريخ الانتهاء:** حدد تاريخ بعد سنة من اليوم

#### الخطوة 4: إنشاء الترخيص

1. اضغط **"Generate License File"**
2. احفظ الملف بامتداد `.sirajelic`
3. أرسل الملف للعميل

---

## 📋 خطوات التوزيع

### 1. بناء البرنامج

```powershell
# الانتقال لمجلد المشروع
cd "C:\SIRAJE CSharp"

# بناء البرنامج
dotnet publish -c Release -r win-x64 --self-contained true
```

### 2. إنشاء ملف التثبيت (اختياري)

إذا كان لديك Inno Setup:

```powershell
# تشغيل Inno Setup Compiler
& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "SIRAJE-Setup.iss"
```

### 3. ضغط الملفات

```powershell
# إنشاء ملف ZIP
$date = Get-Date -Format "yyyyMMdd"
Compress-Archive -Path "bin\Release\net10.0-windows\win-x64\publish\*" `
                 -DestinationPath "SIRAJE-v1.1.1-$date.zip" `
                 -Force
```

---

## 🚀 التثبيت عند العميل

### الخطوة 1: نسخ الملفات

1. فك ضغط ملف ZIP
2. انسخ جميع الملفات إلى مجلد على جهاز العميل
   - مثال: `C:\Program Files\SIRAJE\`

### الخطوة 2: التشغيل الأول

1. شغّل `InventoryManagement.exe`
2. ستظهر شاشة التفعيل
3. انسخ **كود الجهاز (Hardware Hash)**

### الخطوة 3: إنشاء الترخيص

1. استخدم كود الجهاز لإنشاء ترخيص
2. أرسل ملف `.sirajelic` للعميل

### الخطوة 4: تفعيل الترخيص

1. على جهاز العميل، اضغط **"اختيار ملف الترخيص"**
2. اختر ملف `.sirajelic`
3. اضغط **"تفعيل"**
4. سيبدأ البرنامج تلقائياً

---

## 🔒 أمان المفاتيح

### المفتاح الخاص (Private Key)

**الموقع الآمن:**
```
C:\Users\[USERNAME]\SIRAJE_Secure_Keys\private_key.xml
```

**قواعد الأمان:**
- ✅ احفظه خارج المشروع
- ✅ لا ترفعه إلى GitHub
- ✅ لا تشاركه مع أي شخص
- ✅ احتفظ بنسخة احتياطية مشفرة

### المفتاح العام (Public Key)

- ✅ مضمّن في البرنامج (Hardcoded)
- ✅ لا يتم تحميله من ملف خارجي
- ✅ آمن للمشاركة

---

## 🧪 اختبار البرنامج

### اختبار 1: التشغيل

```powershell
cd "C:\SIRAJE CSharp\bin\Release\net10.0-windows\win-x64\publish"
.\InventoryManagement.exe
```

**المتوقع:** شاشة التفعيل تظهر

### اختبار 2: التفعيل

1. احصل على Hardware Hash من الشاشة
2. أنشئ ترخيص اختباري
3. فعّل الترخيص
4. تحقق من ظهور شاشة تسجيل الدخول

### اختبار 3: الحماية من تغيير التاريخ

1. شغّل البرنامج مرة
2. أغلق البرنامج
3. غيّر تاريخ النظام للخلف بيوم
4. حاول تشغيل البرنامج
5. **المتوقع:** رسالة خطأ ورفض التشغيل

---

## 📊 معلومات البرنامج

### حجم البرنامج

- **ملف التثبيت:** ~200 MB
- **بعد التثبيت:** ~250 MB
- **قاعدة البيانات:** تبدأ من 5 MB

### المتطلبات

- **نظام التشغيل:** Windows 10 أو أحدث (64-bit)
- **الذاكرة:** 2 GB RAM (موصى به: 4 GB)
- **المساحة:** 500 MB

---

## 🔄 التحديثات المستقبلية

### عند نشر تحديث جديد

1. **لا تغيّر المفاتيح!** - التراخيص الحالية ستتوقف
2. احتفظ بنفس `private_key.xml` و `public_key.xml`
3. قم بالبناء والنشر كالمعتاد
4. التراخيص الحالية ستعمل مع الإصدار الجديد

### إشعار العملاء

عند توفر تحديث:
1. أرسل إشعار للعملاء
2. وفّر ملف التثبيت الجديد
3. أخبرهم أن الترخيص سيبقى كما هو
4. أخبرهم أن البيانات لن تُحذف

---

## 📞 الدعم الفني

### المشاكل الشائعة

#### "ملف الترخيص غير صالح"
- **السبب:** Hardware Hash غير صحيح
- **الحل:** تأكد من نسخ Hardware Hash بشكل صحيح

#### "الترخيص ليس لهذا الجهاز"
- **السبب:** Hardware Hash غير متطابق
- **الحل:** أنشئ ترخيص جديد بالـ Hardware Hash الصحيح

#### "تم اكتشاف تلاعب في التاريخ"
- **السبب:** تاريخ النظام تم تغييره للخلف
- **الحل:** اضبط تاريخ النظام للوقت الصحيح

#### "الترخيص منتهي"
- **السبب:** انتهت السنة
- **الحل:** أنشئ ترخيص جديد

---

## ✅ قائمة التحقق النهائية

قبل التوزيع، تأكد من:

- [ ] تم بناء البرنامج بنجاح
- [ ] تم اختبار البرنامج على جهاز نظيف
- [ ] تم اختبار إنشاء الترخيص
- [ ] تم اختبار تفعيل الترخيص
- [ ] تم اختبار الحماية من تغيير التاريخ
- [ ] تم حفظ نسخة احتياطية من المفتاح الخاص
- [ ] تم حذف جميع ملفات الاختبار
- [ ] تم إنشاء ملف ZIP أو مثبت

---

## 📝 ملاحظات مهمة

### للموزعين

1. **لا تشارك المفتاح الخاص** مع أي شخص
2. **احفظ المفتاح الخاص** في مكان آمن جداً
3. **لا ترفع المفتاح** إلى GitHub أو أي مكان عام
4. **اعمل نسخة احتياطية** من المفتاح الخاص

### للعملاء

1. **الترخيص مرتبط بالجهاز** - لا يعمل على جهاز آخر
2. **الترخيص سنوي** - يحتاج تجديد كل سنة
3. **لا تغيّر تاريخ النظام** - البرنامج سيرفض العمل
4. **البيانات محلية** - محفوظة على الجهاز فقط

---

## 📅 معلومات الإصدار

**التاريخ:** ديسمبر 2025  
**الإصدار:** v1.1.1  
**نوع الترخيص:** تجربة مجانية + سنوي فقط

---

**بالتوفيق! 🎉**
